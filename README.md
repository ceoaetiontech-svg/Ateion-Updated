
  # Add image scrolling animation

  This is a code bundle for Add image scrolling animation. The original project is available at https://www.figma.com/design/SYJMC8V2P9xrrqduMnfyhG/Add-image-scrolling-animation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  